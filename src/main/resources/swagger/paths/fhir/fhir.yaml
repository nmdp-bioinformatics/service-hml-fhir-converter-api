/fhir:
  post:
    operationId: convert
    parameters:
      -
        in: body
        name: fhirMessage
        required: true
        schema:
          $ref: '#/definitions/FhirMessage'
    responses:
      200:
        schema:
          type: boolean
      500:
        schema:
          $ref: '#/definitions/Error'
        title: ErrorException