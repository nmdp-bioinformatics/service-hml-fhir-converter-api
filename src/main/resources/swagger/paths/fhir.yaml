/fhir:
  post:
    operationId: convert
    parameters:
      -
        in: body
        name: fhirMessages
        required: true
        schema:
          items:
            $ref: '#/definitions/FhirMessage'
          type: array
    responses:
      200:
        schema:
          type: boolean
      500:
        schema:
          $ref: '#/definitions/Error'
        title: ErrorException